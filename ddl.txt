CREATE TABLE USUARIO (ID  SERIAL NOT NULL, LOGIN VARCHAR(255), NOME VARCHAR(255), SENHA VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE IMAGEM (ID  SERIAL NOT NULL, BYTESIMAGEM BYTEA, NOME VARCHAR(255), TEMA_ID INTEGER, USUARIO_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE TEMA (ID  SERIAL NOT NULL, TEMA VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE IMAGEM ADD CONSTRAINT FK_IMAGEM_TEMA_ID FOREIGN KEY (TEMA_ID) REFERENCES TEMA (ID)
ALTER TABLE IMAGEM ADD CONSTRAINT FK_IMAGEM_USUARIO_ID FOREIGN KEY (USUARIO_ID) REFERENCES USUARIO (ID)
